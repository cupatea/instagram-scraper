mutation SignUpUser($password: String!, $username: String!) {
  signUpUser( password: $password, username: $username) {
    success
    errors
    token {
      accessToken
      client
      expiry
      tokenType
      uid
    }
  }
}
