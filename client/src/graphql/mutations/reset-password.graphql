mutation ResetPassword($username: String!, $newPassword: String!, $resetPasswordCode: String!) {
  resetPassword(username: $username, newPassword: $newPassword, resetPasswordCode: $resetPasswordCode) {
    success
    errors
    token {
      accessToken
      client
      expiry
      tokenType
      uid
    }
  }
}
